# Port the backend server listens on
PORT=5000

# Origin(s) allowed to connect (comma separated). Use * for dev.
CLIENT_ORIGIN=http://localhost:5173

# Path to the SQLite database file
DB_PATH=./db/chat.sqlite3
